make_tank:
  cmd: make
  args:
    type: tank
  key: q
  sprite: button_tank

make_sidecar:
  cmd: make
  args:
    type: sidecar
  key: q
  sprite: button_sidecar

move:
  cmd: move
  key: m
  sprite: button_move
  selector: crosshairs

stop:
  cmd: stop
  key: t 
  sprite: button_stop
  selector: crosshairs

attack:
  cmd: attackmove
  cmd_with_target: attack
  key: a
  selector: unitpicker

